# pitch_guide_builder/requirements.txt
python-multipart
fastapi
uvicorn[standard]
pydantic

numpy
scipy

# Audio I/O + resampling
soundfile
resampy

# F0 baseline + fallback spectrograms
librosa

# Preferred fast spectrograms/features
audioflux

# NOTE: CPU-only default image.
# torch/torchcrepe are optional and intentionally excluded to keep builds lighter/faster.
# If you want torchcrepe, install optional deps manually in a custom image.
